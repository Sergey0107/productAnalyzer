# Web framework - FastAPI выбран за его производительность,
# автоматическую документацию API и встроенную валидацию данных
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Работа с файлами - python-multipart необходим для обработки multipart/form-data,
# что используется при загрузке файлов через HTTP
python-multipart==0.0.9

# Работа с PDF файлами - PyMuPDF (fitz) выбран за отличную поддержку
# извлечения текста и таблиц из PDF, работает быстрее аналогов
PyMuPDF==1.24.13

# Интеграция с OpenAI - официальная библиотека для работы с GPT моделями
# Поддерживает как текстовые запросы, так и работу с изображениями
openai==1.54.5

# Интеграция с Anthropic Claude - для использования Claude моделей
# Claude 3.5 Sonnet отлично работает с vision задачами
anthropic==0.39.0

# Переменные окружения - используется для безопасного хранения API ключей
# вместо хардкода в коде
python-dotenv==1.0.1

# CORS middleware - позволяет фронтенду обращаться к API с другого домена/порта
# Необходимо для разработки когда фронтенд и бэкенд на разных портах
